<%= form_with(model: islander, local: true) do |form| %>
  <% if islander.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(islander.errors.count, "error") %> prohibited this islander from being saved:</h2>

      <ul>
        <% islander.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :timezone %>
    <%= form.text_field :timezone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
